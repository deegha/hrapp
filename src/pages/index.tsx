import { InputField, Button, Layout } from "@/components";

export default function Home() {
  return (
    <Layout>
      <div className="h-[100vh] w-full bg-secondary flex items-start justify-center">
        logeg in
      </div>
    </Layout>
  );
}
